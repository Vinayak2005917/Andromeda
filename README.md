# Andromeda Agent

This is a simple langchain agent that can read files from a directory, read specific files, write to files, perform web searches, and summarize webpages for specific queries. It uses the OpenAI API for natural language processing.